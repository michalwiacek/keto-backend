version: "2"
options:
  verbose: true
syncs:
  keto-backend-rsync-sync:
    src: './keto-backend/'
    sync_host_ip: 'auto'
    sync_host_port: 10872
    sync_strategy: 'rsync'
    sync_excludes: ['tmp','.git','coverage','log']